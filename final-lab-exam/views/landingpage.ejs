<section class="hero">
  <div class="hero__overlay"></div>
  <video playsinline="playsinline" autoplay="autoplay" muted="muted" loop="loop" loading="lazy" class="hero__video">
      <source src="images/landing_page_video.mp4" type="video/mp4">
  </video>
  <div id="home" class="hero__content h-100 container-custom position-relative">
      <div class="d-flex h-100 align-items-center hero__content-width">
          <div class="text-white">
              <h1 class="hero__heading fw-bold mb-4">Welcome to EG Travels</h1>
              <p class="lead mb-4">Dream, Explore, Discover</p>
              <% if (user) { %>
                  <span class="mt-2 btn btn-lg btn-outline-light" role="button"><%= user.username %></span>
              <% } else { %>
                  <a href="/register" class="mt-2 btn btn-lg btn-outline-light" role="button">Register Now</a>
              <% } %>
              <a href="/ajax" class="mt-2 btn btn-lg btn-outline-light" role="button">Learn More</a>
          </div>
      </div>
  </div>
</section>
<section class="featured-products py-5">
    <div class="container">
        <h2 class="text-center mb-4">Featured Products</h2>
        <div class="row">
            <% featuredProducts.forEach(product => { %>
                <div class="col-md-4 mb-4">
                    <div class="card product-card shadow-sm">
                        <div class="card-body">
                            <h3 class="card-title"><%= product.name %></h3>
                            <p class="card-text"><%= product.description %></p>
                            <p class="card-text"><strong>Price: $<%= product.price %></strong></p>
                            <a href="/products/<%= product._id %>" class="btn btn-primary">View Product</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</section>